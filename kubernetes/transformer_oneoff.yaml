kind: Job
apiVersion: apps/v1
metadata:
  name: stoq-transformer-oneoff
  labels:
    app: stoq-transformer-oneoff
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stoq-transformer-oneoff
  template:
    metadata:
      labels:
        app: stoq-transformer-oneoff
    spec:
      volumes:
        - name: malware
          persistentVolumeClaim:
            claimName: stoq-raw-malware-pvc
      containers:
        - name: stoq-transformer
          image: gclef/stoq-transformer:0.5
          imagePullPolicy: "IfNotPresent"
          resources:
            limits:
              memory: "3Gi"
            requests:
              memory: "2Gi"
          volumeMounts:
            - name: malware
              mountPath: /malware
              readOnly: True
          env:
            - name: ES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: stoq-transformer-secrets
                  key: ES_USERNAME
            - name: ES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: stoq-transformer-secrets
                  key: ES_PASSWORD
            - name: ES_HOST
              valueFrom:
                secretKeyRef:
                  name: stoq-transformer-secrets
                  key: ES_HOST
            - name: TRANSFORMERS
              valueFrom:
                secretKeyRef:
                  name: stoq-transformer-secrets
                  key: TRANSFORMERS
            - name: PROCESS_ARCHIVE
              value: "true"
      imagePullSecrets:
        - name: registrycreds